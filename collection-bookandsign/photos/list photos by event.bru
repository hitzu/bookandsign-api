meta {
  name: list photos by event
  type: http
  seq: 1
}

get {
  url: {{base_url}}/photos/event/{{event_token}}
  body: none
  auth: inherit
}

headers {
  ~Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
