meta {
  name: presign photo (501 stub)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/photos/presign
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
