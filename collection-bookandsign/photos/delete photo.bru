meta {
  name: delete photo
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/photos/{{id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

vars:pre-request {
  id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
