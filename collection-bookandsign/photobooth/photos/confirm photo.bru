meta {
  name: confirm photo
  type: http
  seq: 3
}

post {
  url: {{base_url}}/sessions/photos/confirm
  body: json
  auth: none
}

body:json {
  {
    "photoId": {{session_photo_id}}
  }
}

vars:pre-request {
  session_photo_id: 779
}

settings {
  encodeUrl: true
  timeout: 0
}
