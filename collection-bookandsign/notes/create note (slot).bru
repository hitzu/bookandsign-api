meta {
  name: create note (slot)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "targetId": 2,
    "kind": "internal",
    "content": "Called customer and confirmed details"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
