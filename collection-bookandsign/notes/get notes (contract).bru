meta {
  name: get notes (contract)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/notes/contract/17?kind=public
  body: json
  auth: inherit
}

params:query {
  kind: public
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
