meta {
  name: get products with filters (term)
  type: http
  seq: 8
}

get {
  url: {{base_url}}/packages?term=Basi
  body: json
  auth: inherit
}

params:query {
  term: Basi
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
