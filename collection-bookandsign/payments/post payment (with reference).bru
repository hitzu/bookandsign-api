meta {
  name: post payment (with reference)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/contracts/{{contract_id}}/payments
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "amount": 75,
    "method": "transfer",
    "receivedAt": "2030-01-01T11:00:00.000Z",
    "reference": "bank-tx-123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

