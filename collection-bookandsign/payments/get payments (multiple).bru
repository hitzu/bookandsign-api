meta {
  name: get payments (multiple)
  type: http
  seq: 7
}

get {
  url: {{base_url}}/contracts/:contract_id/payments
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

