meta {
  name: set promotion packages (bulk)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/promotions/{{promotion_id}}/packages/bulk
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "packageIds": [{{package_id}}]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

