meta {
  name: set promotion packages (bulk)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/promotions/1/packages/bulk
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "packageIds": []
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
