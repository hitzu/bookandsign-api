meta {
  name: create brand
  type: http
  seq: 1
}

post {
  url: {{base_url}}/brands
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Brillipoint"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
