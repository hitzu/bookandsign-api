meta {
  name: 03 - get active promotion for brand (front display)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/promotions?brandId={{brand_id}}&status=active
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
