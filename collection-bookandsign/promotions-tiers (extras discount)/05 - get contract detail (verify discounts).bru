meta {
  name: 05 - get contract detail (verify discounts)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/contracts/{{contract_id}}
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
