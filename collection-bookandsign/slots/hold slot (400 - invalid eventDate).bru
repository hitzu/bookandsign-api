meta {
  name: hold slot (400 - invalid eventDate)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/slots/hold
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "eventDate": "15-01-2026",
    "period": "morning",
    "leadName": "Test Lead"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
