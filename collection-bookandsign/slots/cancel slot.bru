meta {
  name: cancel slot
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/slots/3
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
