meta {
  name: hold slot
  type: http
  seq: 2
}

post {
  url: {{base_url}}/slots/hold
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "eventDate": "2030-10-08",
    "period": "am_block"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
