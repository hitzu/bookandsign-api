meta {
  name: update slot
  type: http
  seq: 12
}

patch {
  url: {{base_url}}/slots/13/lead-info
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "leadName": "testing 1"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
