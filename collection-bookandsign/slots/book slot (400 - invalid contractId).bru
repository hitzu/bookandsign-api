meta {
  name: book slot (400 - invalid contractId)
  type: http
  seq: 7
}

patch {
  url: {{base_url}}/slots/1/book
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "contractId": 0
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
