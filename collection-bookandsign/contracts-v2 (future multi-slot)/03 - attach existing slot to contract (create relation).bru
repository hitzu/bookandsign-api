meta {
  name: 03 - attach existing slot to contract (create relation)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/contracts/12/slots
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": 17,
    "purpose": "trial_makeup"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
