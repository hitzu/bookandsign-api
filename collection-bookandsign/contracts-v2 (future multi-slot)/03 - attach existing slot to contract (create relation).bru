meta {
  name: 03 - attach existing slot to contract (create relation)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/contracts/:contract_id/slots
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": 1,
    "purpose": "event"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

