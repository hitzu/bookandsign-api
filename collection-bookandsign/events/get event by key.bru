meta {
  name: get event by key
  type: http
  seq: 3
}

get {
  url: {{base_url}}/events/by-key/{{event_key}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

vars:pre-request {
  event_key: 17
}

settings {
  encodeUrl: true
  timeout: 0
}
