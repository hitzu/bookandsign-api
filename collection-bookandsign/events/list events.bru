meta {
  name: list events
  type: http
  seq: 5
}

get {
  url: {{base_url}}/events
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
