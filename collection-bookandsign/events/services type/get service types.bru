meta {
  name: get service types
  type: http
  seq: 10
}

get {
  url: {{base_url}}/events/service-types
  body: none
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
