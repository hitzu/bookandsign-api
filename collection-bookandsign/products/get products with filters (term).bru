meta {
  name: get products with filters (term)
  type: http
  seq: 8
}

get {
  url: {{base_url}}/products?term=Face
  body: json
  auth: inherit
}

params:query {
  term: Face
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
