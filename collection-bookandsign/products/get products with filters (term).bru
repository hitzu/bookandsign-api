meta {
  name: get products with filters (term)
  type: http
  seq: 8
}

get {
  url: {{base_url}}/products?term=Renta
  body: json
  auth: inherit
}

params:query {
  term: Renta
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
