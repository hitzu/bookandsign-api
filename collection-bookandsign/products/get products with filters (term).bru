meta {
  name: get products with filters (term)
  type: http
  seq: 8
}

get {
  url: {{base_url}}/products?brandId=2
  body: json
  auth: inherit
}

params:query {
  brandId: 2
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
