meta {
  name: create contract from slot (400 - invalid slotId type)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": "abc",
    "packages": [
      {
        "packageId": {{packageId}},
        "quantity": 1,
        "source": "package"
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}


