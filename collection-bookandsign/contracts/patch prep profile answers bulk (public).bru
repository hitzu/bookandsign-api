meta {
  name: patch prep profile answers bulk (public)
  type: http
  seq: 27
}

patch {
  url: {{base_url}}/contracts/public/{{contract_token}}/prep-profile/answers?phone={{contract_phone}}
  body: json
  auth: none
}

body:json {
  {
    "answers": [
      { "questionId": "event_start_time", "value": "17:00" },
      { "questionId": "event_end_time", "value": "23:00" },
      { "questionId": "venue_type", "value": "hotel" }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

