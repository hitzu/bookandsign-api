meta {
  name: get payments (no payments)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/contracts/{{contract_id}}/payments
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

