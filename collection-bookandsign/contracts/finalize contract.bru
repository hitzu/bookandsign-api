meta {
  name: finalize contract
  type: http
  seq: 6
}

post {
  url: {{base_url}}/contracts/:contractId/finalize
  body: json
  auth: inherit
}

params:path {
  contractId: 3
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
