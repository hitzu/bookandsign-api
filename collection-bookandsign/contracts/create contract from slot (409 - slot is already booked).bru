meta {
  name: create contract from slot (409 - slot is already booked)
  type: http
  seq: 12
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": {{slotIdAlreadyBooked}},
    "packages": [
      {
        "packageId": {{packageId}},
        "quantity": 1,
        "source": "package"
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}


