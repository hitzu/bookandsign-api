meta {
  name: create contract from slot (409 - slot is not held)
  type: http
  seq: 11
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": {{slotIdNotHeld}},
    "packages": [
      {
        "packageId": {{packageId}},
        "quantity": 1,
        "source": "package"
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}


