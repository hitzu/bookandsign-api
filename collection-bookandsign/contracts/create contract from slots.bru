meta {
  name: create contract from slot
  type: http
  seq: 1
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": 1,
    "packages": [
      {
        "packageId": 2,
        "quantity": 2,
        "source": "package"
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
