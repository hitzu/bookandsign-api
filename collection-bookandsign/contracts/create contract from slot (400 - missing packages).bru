meta {
  name: create contract from slot (400 - missing packages)
  type: http
  seq: 8
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "slotId": {{slotId}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}


