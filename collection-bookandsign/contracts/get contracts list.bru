meta {
  name: get contracts list
  type: http
  seq: 15
}

get {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

vars:pre-request {
  contract_id: 5
}

settings {
  encodeUrl: true
  timeout: 0
}
