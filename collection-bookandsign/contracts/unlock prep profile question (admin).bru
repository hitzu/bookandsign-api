meta {
  name: unlock prep profile question (admin)
  type: http
  seq: 23
}

post {
  url: {{base_url}}/contracts/{{contract_id}}/prep-profile/unlock
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "questionId": "wedding_concept_style",
    "reason": "Cliente pidió ajustar estilo"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

