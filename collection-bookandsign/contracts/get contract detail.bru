meta {
  name: get contract detail
  type: http
  seq: 2
}

get {
  url: {{base_url}}/contracts/:contract_id
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

