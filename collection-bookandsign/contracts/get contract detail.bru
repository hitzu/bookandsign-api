meta {
  name: get contract detail
  type: http
  seq: 2
}

get {
  url: {{base_url}}/contracts/5
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

vars:pre-request {
  contract_id: 5
}

settings {
  encodeUrl: true
  timeout: 0
}
