meta {
  name: cancel contract
  type: http
  seq: 4
}

post {
  url: {{base_url}}/contracts/:contract_id/cancel
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

