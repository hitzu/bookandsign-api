meta {
  name: get prep profile (public)
  type: http
  seq: 20
}

get {
  url: {{base_url}}/contracts/public/{{contract_token}}/prep-profile?phone={{contract_phone}}
  body: json
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

