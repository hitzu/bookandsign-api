meta {
  name: reopen contract
  type: http
  seq: 5
}

post {
  url: {{base_url}}/contracts/:contract_id/reopen
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

