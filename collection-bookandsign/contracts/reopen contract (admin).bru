meta {
  name: reopen contract (admin)
  type: http
  seq: 6
}

post {
  url: {{base_url}}/contracts/{{contractId}}/reopen
  auth: inherit
}

headers {
  Authorization: Bearer {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}


