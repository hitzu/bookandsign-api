meta {
  name: create contract
  type: http
  seq: 1
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "userId": 2,
    "slotId": 18,
    "sku": "SKU-DEMO-001",
    "clientName": "Hitzu",
    "subtotal": 4500,
    "discountTotal": 200,
    "total": 4300,
    "packages": [
      { "packageId": 1, "quantity": 1, "basePriceSnapshot": 4300 }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
