meta {
  name: create contract
  type: http
  seq: 1
}

post {
  url: {{base_url}}/contracts
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "userId": 2,
    "slotId": 2,
    "sku": "SKU-DEMO-001",
    "clientName": "Hitzu",
    "subtotal": 4300,
    "discountTotal": 200,
    "total": 4500,
    "packages": [
      { "packageId": 1, "quantity": 1 }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
