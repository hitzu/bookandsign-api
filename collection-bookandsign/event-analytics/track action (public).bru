meta {
  name: track action (public)
  type: http
  seq: 1
}

post {
  url: {{base_url}}/event-analytics/track
  body: json
  auth: none
}

body:json {
  {
    "action": "gallery_opened",
    "source": "qr",
    "eventToken": "{{event_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
