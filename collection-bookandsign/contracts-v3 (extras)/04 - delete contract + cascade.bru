meta {
  name: 04 - delete contract + cascade
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/contracts/{{contract_id}}
  body: none
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
